workflows:
  ios-native-quick-start:
    name: iOS Native
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: 'Rope & Rounds'
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.gamzeay.ropeandrounds
      vars:
        BUNDLE_ID: "com.gamzeay.ropeandrounds"
        XCODE_WORKSPACE: "Rope and Rounds.xcodeproj"
        XCODE_SCHEME: "Rope and Rounds"
        APP_STORE_APPLE_ID: 6809423067
      xcode: latest
      cocoapods: default
    scripts:
      - name: Set up provisioning profiles settings on Xcode project
        script: xcode-project use-profiles
      - name: Build ipa for distribution
        script: |
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
